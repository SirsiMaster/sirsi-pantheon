name: pantheon-ane
display_name: "Pantheon ANE/Metal/MLX"
repo_path: "~/Development/sirsi-pantheon"
deadline: ""
priority: "P2"
max_turns: 50
scope_of_work: |
  Existing files: internal/brain/inference.go (CGO stub), internal/seba/accel.go (Metal stub).
  Read those files first to understand current state, then execute in order:

  1. internal/brain/inference.go — extend the existing CGO bridge to call CoreML's MLModel.prediction(from:) via C interop. Add build tags for darwin/arm64. If CoreML headers are missing, create a minimal Objective-C bridge file.
  2. internal/seba/accel.go — implement Metal compute shader dispatch for parallel SHA-256 hashing. Use the Metal Performance Shaders framework via CGO. Add fallback to pure-Go when Metal is unavailable.
  3. Add a `pantheon benchmark` CLI command in cmd/pantheon/benchmark.go that runs inference.go and accel.go with timing, comparing ANE vs Metal vs CPU paths. Output a table with lipgloss styling.
  4. Skip tokenization and KV cache tasks for now (deferred to next sprint).
  5. Run go build ./cmd/pantheon/ and go test ./... to verify.
  6. Commit, push, run pantheon thoth compact.
