name: CI — Sirsi Pantheon

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --timeout=5m

  test:
    name: Test
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Run tests
        shell: bash
        run: go test -v -race -short -coverprofile=coverage.out ./...
        env:
          CGO_ENABLED: 1

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out

      - name: Ma'at Pulse — Dynamic Metrics
        shell: bash
        run: |
          go build -o pantheon ./cmd/pantheon/
          ./pantheon maat pulse --skip-test --json > metrics.json
          echo "=== Ma'at Pulse Metrics ==="
          cat metrics.json

      - name: Upload metrics
        uses: actions/upload-artifact@v4
        with:
          name: metrics
          path: metrics.json

  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go-version: ['1.25']
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Build pantheon
        run: go build -o pantheon${{ matrix.os == 'windows-latest' && '.exe' || '' }} ./cmd/pantheon/

      - name: Build pantheon-agent
        run: go build -o pantheon-agent${{ matrix.os == 'windows-latest' && '.exe' || '' }} ./cmd/pantheon-agent/
        env:
          CGO_ENABLED: '0'

      - name: Binary size guard (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          PANTHEON_SIZE=$(stat -f%z pantheon 2>/dev/null || stat -c%s pantheon)
          AGENT_SIZE=$(stat -f%z pantheon-agent 2>/dev/null || stat -c%s pantheon-agent)
          echo "pantheon: ${PANTHEON_SIZE} bytes"
          echo "pantheon-agent: ${AGENT_SIZE} bytes"
          if [ "$PANTHEON_SIZE" -gt 26214400 ]; then
            echo "::warning::pantheon binary exceeds 25MB (${PANTHEON_SIZE} bytes)"
          fi
          if [ "$AGENT_SIZE" -gt 12582912 ]; then
            echo "::warning::pantheon-agent binary exceeds 12MB (${AGENT_SIZE} bytes)"
          fi

      - name: Binary size guard (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          $p_size = (Get-Item pantheon.exe).Length
          $a_size = (Get-Item pantheon-agent.exe).Length
          Write-Host "pantheon: $p_size bytes"
          Write-Host "pantheon-agent: $a_size bytes"
          if ($p_size -gt 26214400) { Write-Output "::warning::pantheon binary exceeds 25MB ($p_size bytes)" }
          if ($a_size -gt 12582912) { Write-Output "::warning::pantheon-agent binary exceeds 12MB ($a_size bytes)" }
