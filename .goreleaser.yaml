# goreleaser config — Sirsi Pantheon v0.8.0-beta
# Docs: https://goreleaser.com
version: 2

project_name: sirsi-pantheon

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: pantheon
    main: ./cmd/pantheon/
    binary: pantheon
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}

  - id: pantheon-agent
    main: ./cmd/pantheon-agent/
    binary: pantheon-agent
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}

  - id: pantheon-anubis
    main: ./cmd/anubis/
    binary: pantheon-anubis
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}

  - id: pantheon-maat
    main: ./cmd/maat/
    binary: pantheon-maat
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}

  - id: pantheon-thoth
    main: ./cmd/thoth/
    binary: pantheon-thoth
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}

  - id: pantheon-scarab
    main: ./cmd/scarab/
    binary: pantheon-scarab
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}

  - id: pantheon-guard
    main: ./cmd/guard/
    binary: pantheon-guard
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    builds:
      - pantheon
      - pantheon-agent
      - pantheon-anubis
      - pantheon-maat
      - pantheon-thoth
      - pantheon-scarab
      - pantheon-guard
    name_template: >-
      sirsi-pantheon_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
      - goos: linux
        format: tar.gz
      - goos: darwin
        format: tar.gz
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md
      - configs/default_rules.yaml
      - configs/example_policy.yaml
      - configs/workspace_template.yaml

  - id: pantheon-anubis
    builds:
      - pantheon-anubis
    name_template: >-
      pantheon-anubis_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
      - goos: linux
        format: tar.gz
      - goos: darwin
        format: tar.gz
    files:
      - LICENSE

  - id: pantheon-maat
    builds:
      - pantheon-maat
    name_template: >-
      pantheon-maat_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
      - goos: linux
        format: tar.gz
      - goos: darwin
        format: tar.gz
    files:
      - LICENSE

  - id: pantheon-thoth
    builds:
      - pantheon-thoth
    name_template: >-
      pantheon-thoth_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
      - goos: linux
        format: tar.gz
      - goos: darwin
        format: tar.gz
    files:
      - LICENSE

  - id: pantheon-scarab
    builds:
      - pantheon-scarab
    name_template: >-
      pantheon-scarab_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
      - goos: linux
        format: tar.gz
      - goos: darwin
        format: tar.gz
    files:
      - LICENSE

  - id: pantheon-guard
    builds:
      - pantheon-guard
    name_template: >-
      pantheon-guard_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
      - goos: linux
        format: tar.gz
      - goos: darwin
        format: tar.gz
    files:
      - LICENSE

brews:
  - name: sirsi-pantheon
    ids:
      - default
    repository:
      owner: SirsiMaster
      name: homebrew-tools
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/SirsiMaster/sirsi-pantheon
    description: "Unified DevOps Intelligence Platform — One Install, All Deities"
    license: Apache-2.0
    install: |
      bin.install "pantheon"
      bin.install "pantheon-agent"
      bin.install "pantheon-anubis"
      bin.install "pantheon-maat"
      bin.install "pantheon-thoth"
      bin.install "pantheon-scarab"
      bin.install "pantheon-guard"
    test: |
      system "#{bin}/pantheon", "version"

  - name: pantheon-anubis
    ids:
      - pantheon-anubis
    repository:
      owner: SirsiMaster
      name: homebrew-tools
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/SirsiMaster/sirsi-pantheon
    description: "Infrastructure scanner and cleaner — standalone deity"
    license: Apache-2.0
    install: |
      bin.install "pantheon-anubis"
    test: |
      system "#{bin}/pantheon-anubis", "version"

  - name: pantheon-maat
    ids:
      - pantheon-maat
    repository:
      owner: SirsiMaster
      name: homebrew-tools
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/SirsiMaster/sirsi-pantheon
    description: "Quality assurance and audit engine — standalone deity"
    license: Apache-2.0
    install: |
      bin.install "pantheon-maat"
    test: |
      system "#{bin}/pantheon-maat", "version"

  - name: pantheon-thoth
    ids:
      - pantheon-thoth
    repository:
      owner: SirsiMaster
      name: homebrew-tools
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/SirsiMaster/sirsi-pantheon
    description: "AI project memory with context compression — standalone deity"
    license: Apache-2.0
    install: |
      bin.install "pantheon-thoth"
    test: |
      system "#{bin}/pantheon-thoth", "version"

  - name: pantheon-scarab
    ids:
      - pantheon-scarab
    repository:
      owner: SirsiMaster
      name: homebrew-tools
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/SirsiMaster/sirsi-pantheon
    description: "Fleet sweep and network discovery — standalone deity"
    license: Apache-2.0
    install: |
      bin.install "pantheon-scarab"
    test: |
      system "#{bin}/pantheon-scarab", "version"

  - name: pantheon-guard
    ids:
      - pantheon-guard
    repository:
      owner: SirsiMaster
      name: homebrew-tools
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/SirsiMaster/sirsi-pantheon
    description: "System resource monitoring with accelerator detection — standalone deity"
    license: Apache-2.0
    install: |
      bin.install "pantheon-guard"
    test: |
      system "#{bin}/pantheon-guard", "version"

checksum:
  name_template: checksums.txt
  algorithm: sha256

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^ci:"
      - "^chore:"

release:
  github:
    owner: SirsiMaster
    name: sirsi-pantheon
  name_template: "v{{.Version}}"
  prerelease: auto
  draft: false
  header: |
    ## Sirsi Pantheon {{.Version}}

    Infrastructure hygiene and developer intelligence. One binary.

    ### Core Features
    - **Scan** — 58 rules across 7 categories find infrastructure waste
    - **Ghosts** — Detect remnants of uninstalled apps (macOS + Linux)
    - **Dedup** — Find duplicate files with 3-phase hashing
    - **Network** — Security audit with DNS safety rollback
    - **Hardware** — CPU, GPU, ANE, CUDA detection
    - **Thoth** — AI project memory via MCP
    - **MCP Server** — Tools for AI IDE integration (Claude, Cursor, Windsurf, VS Code)

    ### Stats
    - 1,663 tests, ~85% coverage, 0 lint errors
    - 27 packages, Apple Silicon first-class
    - Spotlight-accelerated file classification on macOS
    - Real BPE tokenizer for accurate token counting

    ### Install
    ```bash
    brew tap SirsiMaster/tools && brew install sirsi-pantheon
    # or
    go install github.com/SirsiMaster/sirsi-pantheon/cmd/pantheon@{{.Tag}}
    ```
  footer: |
    ---
    *Built by [Sirsi Technologies](https://sirsi.ai). Apache 2.0 licensed.*
