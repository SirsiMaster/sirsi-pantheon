- **`ADR-INDEX.md` claimed ADR-055 was "next available" while ADR-055 was already registered in the same file, and ADR-054 was absent entirely** (claude-home, 2026-08-06). Both ADR-054 documents — `ADR-054-ONE-HORUS-UNIFIED-AGENT-FABRIC.md` and its companion `ADR-054-CONTRACTS-IDENTITY-AND-LEDGER-V7.md` — exist on `main` with **zero** mentions in the index, so the one file whose job is to make ADR numbers unique did not know the largest live number. Two separate lines then advertised `ADR-055` as next available (the header count and the allocation table) while ADR-055 sat registered between them, which is exactly how the 051/054 collision that PR #465 renumbered came to happen in the first place: an allocator that under-reports the high-water mark hands out a number that is already taken. Registered ADR-054 in both the decision table and the allocation table, recorded that the companion contracts share 054 **by design rather than by collision** so a future reader does not "fix" it into 056, and corrected next-available to ADR-056 with the total to 55. Found while auditing the PR #465 forward-fix after its headless build timed out; the remaining forward-fix steps (claims-table rename, stale owner-gate language in ADR-055, and six reproduction scripts named in `docs/REPRODUCTION.md` that do not exist) are reported separately and deliberately NOT bundled here. Refs: PANTHEON_RULES.md A7/A26; PR #465; router item `20260805-234120`; Changelog: Unreleased
