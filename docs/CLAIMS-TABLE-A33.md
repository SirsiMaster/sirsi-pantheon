# Deck claims table — A33 voice — BLESSED

Status: BLESSED by the owner as written, 2026-08-03 ("bless the A33 claims
as written"), relayed by claude-deck (router item
20260803-165513-claude-deck-claude-nexus). This table is now the ONLY
claims source for outward surfaces; all prior claims-package language is
superseded. Nothing outside this table ships. Derives from
sirsi-inference/docs/CONTRIBUTION-ADJUSTED.md under the A33 tone law;
every line links docs/REPRODUCTION.md. Canonical source: sirsi-inference
@ 823d1dc.

## The four blessed claims

1. "In our published benchmark, the engine measured +4.02% faster
   single-stream than the stock toolchain as commonly deployed — 32 of 33
   rounds, independently reviewed. Full parameters and one-command scripts
   are published; we welcome independent runs."

2. "Serving a team, our architecture measured ~1.43x aggregate throughput
   versus a leading open alternative at equal concurrency limits on an
   identical workload — same reproduction pack, same invitation."

3. "We identified potential defects in the underlying framework and
   reported them upstream with reproductions and a proposed fix. With that
   fix shared with every comparator, single-stream performance converges to
   parity — which is why we treat serving architecture, not any single
   primitive, as our durable contribution."

4. "In audit mode, the engine returned bit-identical answers alone or under
   8-way load, 16 of 16 checks, independently reviewed. A correctness
   feature for reproducibility-sensitive work — never a speed claim."

## Mandatory footnote on every public use

"Measured on our hardware (Apple M5 Max, 48 GB, macOS 26.5.2), our
workload, our pinned versions — full environment and scripts in the
verification pack; results elsewhere may legitimately differ and we would
like to hear about them."

## Usage rule

These four claims are the ceiling, not the floor. Use the exact wording;
do not paraphrase, amplify, or combine with prior claims-package language.
Any deviation requires a new owner blessing.

Refs: ADR-051, docs/REPRODUCTION.md, ANUBIS_RULES.md § Rule A33
