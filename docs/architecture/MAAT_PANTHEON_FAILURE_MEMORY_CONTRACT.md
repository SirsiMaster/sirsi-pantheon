# Pantheon Ma’at contract authority

Updated 2026-09-11. Classification: platform-foundation.

This path is a supersession pointer, not a second contract.
The sole Pantheon contract is [Pantheon Ma’at failure memory and operational preflight](../contracts/PANTHEON_MAAT_FAILURE_MEMORY_CONTRACT.md), owned by `codex-pantheon`.

The draft formerly at this path in commit `707c27a36` is superseded. Its proposed `pass/fail/unverified` outcomes and six-field signature are withdrawn. Consumers use the authoritative contract’s `pass/reject/unverifiable` outcomes and its explicitly proposed Pantheon-local signature fields. SNE keys remain opaque and producer-owned; shared normalization still requires producer fixtures and convergence. No SNE wire fields or source are changed by this reconciliation.

The historical draft is preserved at `/Users/thekryptodragon/.sirsi/reviews/software-admin-20260911/maat-contract/0-MAAT_PANTHEON_FAILURE_MEMORY_CONTRACT.md`, SHA-256 `c5da5356980144936dbb1988874eb5f62eba088e1e431003ef23d49a9fe3536b`. It is evidence of a superseded proposal, not implementation authority.

Owner Reading Room entry: `/Users/thekryptodragon/Desktop/Sirsi - Owner Reading Room/Pantheon Maat failure memory contract.md`. That entry now links to and reproduces the sole contract. Native Google Workspace publication remains pending: the required managed document creation/render runtime is unavailable in this session. Recover by rendering and verifying the reading edition, importing a native Google Doc and recording the URL in the canonical contract and Desktop entry.

Independent review remains with `claude-pantheon`; runtime integration remains with `codex-pantheon`. Neither this pointer nor the contract certifies installed enforcement or release qualification. The repo-owner contract’s earlier pending Desktop/Git notes describe its author’s session and are superseded for human access by the current Desktop entry and this delivery record; its contents are otherwise preserved byte-for-byte.
